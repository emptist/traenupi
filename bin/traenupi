#!/usr/bin/env bash
set -euo pipefail

PROJECT_ROOT="/Users/jk/gits/hub/tools_ai/traenupi"
GLEAM_CLI_DIR="$PROJECT_ROOT/gleam/traenupi_cli"

export NODE_PATH="$PROJECT_ROOT/node_modules"

cd "$GLEAM_CLI_DIR"
exec gleam run -- "$@"
